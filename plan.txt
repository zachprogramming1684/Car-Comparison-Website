1. Find data set to use and use CommandLineRunner to import the data on startup
2. confirm that backend is fully implemented and works

FEATURES:
- Users can search for car by name and use advanced queries to select up to 5 cars for comparison.
- Users can also use full screen comparison for each car

3. learn frontend languages and make frontend to interact with backend
    - right now I want this to be a single page with two tables each representing a car
    - the user can use a dropdown or something to pick each car they want to compare
        - then each car is pulled from the database and each table is populated
4. deploy to the cloud


COMPARISONDTO
- ComparisonDTO class to display two cars side by side
    - list of car names
    - list of DifferenceDTO
    - string summary
- DifferenceDTO class
    - String feature
    - double car1Value
    - double car2Value
    - Long winnerId

Cases for summary:
 Price
    - carA has better price than carB
    - carB has better price than carA
    - both prices are equal

Custom Value Score Logic:
- Users can request a single car by id to
